add_executable(glassos
    ../main.cpp
)

target_link_libraries(glassos
    PRIVATE
        Kernel
        Desktop
        WindowManager
        Renderer
        Explorer
        GlassEffect
        Taskbar
        StartMenu
        Terminal
        Settings
)

target_include_directories(glassos
    PRIVATE
        ${PROJECT_SOURCE_DIR}/kernel/include
        ${PROJECT_SOURCE_DIR}/desktop
        ${PROJECT_SOURCE_DIR}/window_manager
        ${PROJECT_SOURCE_DIR}/renderer
        ${PROJECT_SOURCE_DIR}/explorer
        ${PROJECT_SOURCE_DIR}/glass_effect
        ${PROJECT_SOURCE_DIR}/taskbar
        ${PROJECT_SOURCE_DIR}/start_menu
        ${PROJECT_SOURCE_DIR}/terminal
        ${PROJECT_SOURCE_DIR}/settings
)
